{{ 'section-steps.css' | asset_url | stylesheet_tag }}

{% style %}
  {% render 'section-margin' %}
{% endstyle %}

{% liquid
  if section.settings.aspect_ratio == 'adapt-to-image'
    if section.settings.image != blank
      assign padding_bottom = 1 | divided_by: section.settings.image.aspect_ratio | times: 100 | round: 2
    else
      assign padding_bottom = '0'
    endif
  elsif section.settings.aspect_ratio == 'landscape'
    assign padding_bottom = '69.5'
  elsif section.settings.aspect_ratio == 'portrait'
    assign padding_bottom = '131.6'
  elsif section.settings.aspect_ratio == 'recommended'
    assign padding_bottom = '80'
  elsif section.settings.aspect_ratio == 'square'
    assign padding_bottom = '100'
  else
    assign padding_bottom = '0'
  endif
%}

{% liquid
  if section.settings.aspect_ratio_mobile == 'adapt-to-image'
    if section.settings.image != blank
      assign padding_bottom_mobile = 1 | divided_by: section.settings.image.aspect_ratio | times: 100 | round: 2
    else
      assign padding_bottom_mobile = '0'
    endif
  elsif section.settings.aspect_ratio_mobile == 'landscape'
    assign padding_bottom_mobile = '69.5'
  elsif section.settings.aspect_ratio_mobile == 'portrait'
    assign padding_bottom_mobile = '131.6'
  elsif section.settings.aspect_ratio_mobile == 'recommended'
    assign padding_bottom_mobile = '80'
  elsif section.settings.aspect_ratio_mobile == 'square'
    assign padding_bottom_mobile = '100'
  else
    assign padding_bottom_mobile = '0'
  endif
%}

{%- style -%}
  .steps-{{ section.id }} .steps__image-wrapper::before {
    content: '';
    display: block;
    width: 0;
    padding-bottom: {{ padding_bottom_mobile }}%;
  }

  @media (min-width: 990px){
    .steps-{{ section.id }} .steps__image-wrapper::before {
      content: '';
      display: block;
      width: 0;
      padding-bottom: {{ padding_bottom }}%;
    }
  }
{%- endstyle -%}

<div class="steps steps-{{ section.id }}">
  <div class="container">
    <div class="steps__wrapper">
      {% if section.settings.heading != blank %}
        <{{ section.settings.heading_tag | default: 'h3' }} class="steps-heading {{ section.settings.heading_size }} richtext-link-inside">
          {{ section.settings.heading }}
        </{{ section.settings.heading_tag | default: 'h3' }}>
      {% endif %}
      <div class="steps__box">
        {%- if section.blocks.size > 0 -%}
          <div class="steps__blocks">
            {% for block in section.blocks %}
              {% if block.settings.label != blank or block.settings.text != blank %}
                <div class="steps__item">
                  {% if block.settings.label != blank %}
                    <div class="steps__label">
                      {{ block.settings.label | truncate: 6, '' }}
                    </div>
                  {% endif %}
                  {% if block.settings.text != blank %}
                    <div class="steps__text body-large richtext-link-inside">
                      {{ block.settings.text }}
                    </div>
                  {% endif %}
                </div>
              {% endif %}
            {% endfor %}
          </div>
        {%- endif -%}
        {% liquid
          if section.settings.image
            assign image = section.settings.image
          endif
        %}
        <div class="steps__image-block">
          {% if section.settings.side_heading != blank %}
            <h4 class="steps-side-heading parallax-text vertical-text" data-parallax="400">
              {{ section.settings.side_heading | truncate: 16, '' }}
            </h4>
          {% endif %}
          <div class="steps__image-wrapper">
            {% if image != blank %}
              {% comment %} Image settings {% endcomment %}
              {% liquid
                assign image_styles = 'object-fit: cover'

                if section.settings.aspect_ratio == 'adap-to-image'
                  assign settings_aspect_ratio = image.aspect_ratio
                else
                  if section.settings.aspect_ratio == 'landscape'
                    assign settings_aspect_ratio = 1.44
                  elsif section.settings.aspect_ratio == 'portrait'
                    assign settings_aspect_ratio = 0.76
                  elsif section.settings.aspect_ratio == 'recommended'
                    assign settings_aspect_ratio = 1.26
                  else
                    assign settings_aspect_ratio = 1
                  endif
                endif

                assign calc_ratio = 1

                if image.aspect_ratio > settings_aspect_ratio
                  assign calc_ratio = image.aspect_ratio | divided_by: settings_aspect_ratio | round: 2
                endif

                assign image_loading = 'lazy'

                if section.index == 1
                  assign image_loading = 'eager'
                endif
              %}
              {% comment %} TODO: add sizes {% endcomment %}
              {%- capture image_sizes -%}
              (min-width: 750px) calc(50vw * {{ calc_ratio }}), calc(100vw * {{ calc_ratio }})
            {%- endcapture -%}
              {{
                image
                | image_url: width: image.width
                | image_tag:
                  width: image.width,
                  height: image.height,
                  widths: '360, 535, 600, 720, 940, 1066, 1200, 1400, 1600, 1800, 2000, 2200, 2400, 2600, 2800, 3000, 3200, 3400, 3600, 3800',
                  sizes: image_sizes,
                  class: 'steps__image',
                  style: image_styles,
                  loading: image_loading
              }}
            {% else %}
              {%- liquid
                assign placeholder_class = 'placeholder-svg'
                if section.settings.aspect_ratio == 'adapt-to-image'
                  assign placeholder_class = placeholder_class | append: ' ' | append: 'adapt-to-image'
                endif

                if section.settings.aspect_ratio_mobile == 'adapt-to-image'
                  assign placeholder_class = placeholder_class | append: ' ' | append: 'adapt-to-image-mobile'
                endif
              %}
              {{ 'hero-apparel-1' | placeholder_svg_tag: placeholder_class }}
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.steps.name",
  "class": "steps-section",
  "tag": "section",
  "enabled_on": {
    "templates": ["*"]
  },
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:sections.all.heading.label",
      "default": "t:sections.steps.settings.heading.default"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h5",
          "label": "t:sections.all.heading_size.options__5.label"
        },
        {
          "value": "h4",
          "label": "t:sections.all.heading_size.options__4.label"
        },
        {
          "value": "h3",
          "label": "t:sections.all.heading_size.options__3.label"
        },
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__1.label"
        }
      ],
      "default": "h3",
      "label": "t:sections.all.heading_size.label",
      "visible_if": "{{ section.settings.heading != blank }}"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "options": [
        {
          "value": "h1",
          "label": "t:sections.all.heading_tag.options__1.label"
        },
        {
          "value": "h2",
          "label": "t:sections.all.heading_tag.options__2.label"
        },
        {
          "value": "h3",
          "label": "t:sections.all.heading_tag.options__3.label"
        },
        {
          "value": "h4",
          "label": "t:sections.all.heading_tag.options__4.label"
        },
        {
          "value": "h5",
          "label": "t:sections.all.heading_tag.options__5.label"
        },
        {
          "value": "h6",
          "label": "t:sections.all.heading_tag.options__6.label"
        },
        {
          "value": "div",
          "label": "t:sections.all.heading_tag.options__7.label"
        }
      ],
      "default": "h3",
      "label": "t:sections.all.heading_tag.label",
      "info": "t:sections.all.heading_tag.info",
      "visible_if": "{{ section.settings.heading != blank }}"
    },
    {
      "type": "text",
      "id": "side_heading",
      "label": "t:sections.steps.settings.side_heading.label",
      "default": "t:sections.steps.settings.side_heading.default",
      "info": "t:sections.steps.settings.side_heading.info"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.all.image.label"
    },
    {
      "type": "select",
      "id": "aspect_ratio",
      "options": [
        {
          "value": "landscape",
          "label": "t:sections.all.card.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.all.card.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.all.card.image_ratio.options__3.label"
        },
        {
          "value": "adapt-to-image",
          "label": "t:sections.all.card.image_ratio.options__4.label"
        }
      ],
      "default": "square",
      "label": "t:sections.all.aspect_ratio_desktop.label"
    },
    {
      "type": "select",
      "id": "aspect_ratio_mobile",
      "options": [
        {
          "value": "landscape",
          "label": "t:sections.all.card.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.all.card.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.all.card.image_ratio.options__3.label"
        },
        {
          "value": "adapt-to-image",
          "label": "t:sections.all.card.image_ratio.options__4.label"
        }
      ],
      "default": "square",
      "label": "t:sections.all.aspect_ratio_mobile.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.section-margin.header.content"
    },
    {
      "type": "select",
      "id": "margin_top",
      "options": [
        {
          "value": "no-indent",
          "label": "t:sections.all.section-margin.options__1.label"
        },
        {
          "value": "xs",
          "label": "t:sections.all.section-margin.options__2.label"
        },
        {
          "value": "s",
          "label": "t:sections.all.section-margin.options__3.label"
        },
        {
          "value": "m",
          "label": "t:sections.all.section-margin.options__4.label"
        },
        {
          "value": "l",
          "label": "t:sections.all.section-margin.options__5.label"
        },
        {
          "value": "xl",
          "label": "t:sections.all.section-margin.options__6.label"
        }
      ],
      "default": "no-indent",
      "label": "t:sections.all.section-margin.margin_top.label"
    },
    {
      "type": "select",
      "id": "margin_bottom",
      "options": [
        {
          "value": "no-indent",
          "label": "t:sections.all.section-margin.options__1.label"
        },
        {
          "value": "xs",
          "label": "t:sections.all.section-margin.options__2.label"
        },
        {
          "value": "s",
          "label": "t:sections.all.section-margin.options__3.label"
        },
        {
          "value": "m",
          "label": "t:sections.all.section-margin.options__4.label"
        },
        {
          "value": "l",
          "label": "t:sections.all.section-margin.options__5.label"
        },
        {
          "value": "xl",
          "label": "t:sections.all.section-margin.options__6.label"
        }
      ],
      "default": "xl",
      "label": "t:sections.all.section-margin.margin_bottom.label"
    }
  ],
  "blocks": [
    {
      "type": "column",
      "name": "t:sections.multicolumn.blocks.column.name",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "t:sections.steps.blocks.column.settings.label.label",
          "default": "t:sections.steps.blocks.column.settings.label.default",
          "info": "t:sections.steps.blocks.column.settings.label.info"
        },
        {
          "type": "inline_richtext",
          "id": "text",
          "label": "t:sections.all.text.label",
          "default": "t:sections.steps.blocks.column.settings.text.default"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.steps.name",
      "category": "t:sections.categories.storytelling",
      "blocks": [
        {
          "type": "column"
        },
        {
          "type": "column"
        },
        {
          "type": "column"
        },
        {
          "type": "column"
        }
      ]
    }
  ]
}
{% endschema %}
